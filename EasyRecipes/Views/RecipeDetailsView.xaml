<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="EasyRecipes.Views.RecipeDetailsView">
    <ContentPage.Content>
      <StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Padding="5" Spacing="5">
        <Image x:Name="imgProduct" WidthRequest="200" HeightRequest="200">
            <Image.Source>
            <UriImageSource Uri="{Binding RecipeToShow.ProductImage}"
            CacheValidity="2"
            CachingEnabled="True"></UriImageSource>
            </Image.Source>
        </Image>
        <StackLayout Orientation="Vertical" Padding="15">
            <Label x:Name="lblName" Text="{Binding RecipeToShow.Name}" FontSize="20" FontAttributes="Bold"/>
            <StackLayout Orientation="Horizontal" HorizontalOptions="Start">
                <Label Text="by" TextColor="Gray"/>
                <Label x:Name="lblImage" Text="{Binding RecipeToShow.Image}" TextColor="Gray"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="Start">
                <Label x:Name="lblDescription" Text="{Binding RecipeToShow.Description}" TextColor="#3483eb"/>
                <Label Text="/5" TextColor="#3483eb"/>
            </StackLayout>
        </StackLayout>
       </StackLayout>
   </ContentPage.Content>
</ContentPage>

